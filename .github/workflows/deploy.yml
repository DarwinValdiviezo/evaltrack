name: Deploy to Production

on:
  push:
    tags:
      - 'v*'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: backend/package-lock.json
      
      - name: Install backend dependencies
        run: |
          cd backend
          npm ci
      
      - name: Run backend tests
        run: |
          cd backend
          npm run test
        env:
          DATABASE_URL: postgresql://test:test@localhost:5432/test
      
      - name: Setup Node.js for frontend
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json
      
      - name: Install frontend dependencies
        run: |
          cd frontend
          npm ci
      
      - name: Run frontend tests
        run: |
          cd frontend
          npm run test

  build:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      
      # Comentado temporalmente hasta que las imágenes funcionen
      # - name: Build and push backend image
      #   uses: docker/build-push-action@v5
      #   with:
      #     context: ./backend
      #     file: ./backend/Dockerfile.simple
      #     push: true
      #     tags: darwinvaldiviezo/evaltrack-api:latest
      #     cache-from: type=gha
      #     cache-to: type=gha,mode=max
      
      # - name: Build and push frontend image
      #   uses: docker/build-push-action@v5
      #   with:
      #     context: ./frontend
      #     push: true
      #     tags: darwinvaldiviezo/evaltrack-frontend:latest
      #     cache-from: type=gha
      #     cache-to: type=gha,mode=max
      
      # - name: Build and push migration image
      #   uses: docker/build-push-action@v5
      #   with:
      #     context: ./backend
      #     file: ./backend/Dockerfile.migration
      #     push: true
      #     tags: darwinvaldiviezo/evaltrack-migration:latest
      #     cache-from: type=gha
      #     cache-to: type=gha,mode=max

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment: production
    steps:
      - uses: actions/checkout@v4
      
      - name: Deploy to server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSH_KEY }}
          script: |
            # Comentado temporalmente hasta que las imágenes funcionen
            # echo "Pulling latest images..."
            # docker pull darwinvaldiviezo/evaltrack-api:latest
            # docker pull darwinvaldiviezo/evaltrack-frontend:latest
            # docker pull darwinvaldiviezo/evaltrack-migration:latest
            
            # echo "Running database migrations..."
            # docker run --rm \
            #   -e DATABASE_URL="${{ secrets.DATABASE_URL }}" \
            #   darwinvaldiviezo/evaltrack-migration:latest
            
            # echo "Deploying application..."
            # docker-compose -f docker-compose.prod.yml up -d
            
            echo "Deployment temporarily disabled - images not ready"
            echo "Manual deployment required" 